@model AdmCRRN.Models.Membro

    <div class="display-label">Bairro</div>
    <div class="display-field">@Model.Endereco.Bairro</div>

    <div class="display-label">Cep</div>
    <div class="display-field">@Model.Endereco.Cep</div>

    <div class="display-label">Cidade</div>
    <div class="display-field">@Model.Endereco.Cidade</div>

    <div class="display-label">Logradouro</div>
    <div class="display-field">@Model.Endereco.Logradouro</div>

    <div class="display-label">Numero</div>
    <div class="display-field">@Model.Endereco.Numero</div>

    <div class="display-label">Uf</div>
    <div class="display-field">@Model.Endereco.Uf</div>

    <div class="display-label">Complemento</div>
    <div class="display-field">@Model.Endereco.Complemento</div>

